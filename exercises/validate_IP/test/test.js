const chai = require("chai");
const assert = chai.assert;

const test = require("../main.js");

describe("An IP Address" function(){
         it ("should have 3 periods"{
             assert.isTrue(test("123.123.123.123"));
        assert.isTrue(test(1.1.1.1));
        assert.isTrue(test(1.1.1.1));
    asser.isFalse(vali)
    
             })
    
    
    });
});