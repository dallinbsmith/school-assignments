import React from 'react'

const Button = (props) => {
    return (
        <div>
        <hr/>    
        <button className = "btn center-block game_button"> = </button>
        </div>
    )
}
export default Button